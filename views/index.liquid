<!DOCTYPE html>
<html lang="en">
    <head>
        {% include "partials/_analytics.html" %}
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Alec Fernandes &mdash; Portfolio & Blog</title>
        <!-- meta_tags -->
        <meta
            name="type"
            property="og:type"
            content="website" />
        <meta
            name="title"
            property="og:title"
            content="Alec Fernandes &mdash; Portfolio & Blog" />
        <meta
            name="url"
            property="og:url"
            content="https://fern.haus" />
        <meta
            name="description"
            property="og:description"
            content="Writer and web developer. Follow me @alec4nandes on most major socials." />
        <meta name="image"
            property="og:image"
            content="https://fern.haus/assets/ocean-gliderport-background.jpg" />
        <!-- twitter meta tags -->
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:site" content="@alec4nandes" />
        <meta name="twitter:title" content="Alec Fernandes &mdash; Portfolio & Blog" />
        <meta name="twitter:description" content="Writer and web developer. Follow me @alec4nandes on most major socials." />
        <meta name="twitter:image" content="https://fern.haus/assets/ocean-gliderport-background.jpg" />
        <!-- end meta tags -->
        <link
            rel="icon"
            type="image/x-icon"
            href="/assets/logo/favicon.ico" />
        <link rel="stylesheet" href="/css/home.css" />
    </head>
    <body>
        <header>
            <h1>Alec Fernandes</h1>
            <a id="home-contact-button" href="/contact">contact me</a>
        </header>
        <main>
            <section>
                <h2 class="small-link-heading">Latest Posts</h2>
                <a class="read-all-link" href="./blog">(read all)</a>
                <div>
                    {% for post in posts.latest_posts %}
                        {% if forloop.index == 1 %}
                            <div id="newest-post" class="content-bubble post">
                                <a
                                    href="/blog/{{ post.post_id | slugify }}.html"
                                    class="feature-image"
                                    style="background-image: url('{{ post.feature_image }}');"></a>
                                <div class="content">
                                    <h3>
                                        <a href="/blog/{{ post.post_id | slugify }}.html">
                                            {{ post.title }}
                                        </a>
                                    </h3>
                                    <h4>{{ post.subtitle }}</h4>
                                    <p class="date-posted">
                                        {{ post.date }}
                                    </p>
                                    <p>
                                        <a href="/blog/{{ post.post_id | slugify }}.html">(read more)</a>
                                    </p>
                                </div>
                            </div>
                        {% else %}
                            {% if forloop.index == 2 %}
                                {{ "<div id='recent-posts'>" }}
                            {% endif %}
                            <a href="/blog/{{ post.post_id | slugify }}.html">
                                <div>
                                    <div class="project-image" style="background-image: url('{{ post.feature_image }}');"></div>
                                    <span>{{ post.title }}</span>
                                </div>
                            </a>
                            {% if forloop.last == true %}
                                {{ "</div>" }}
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </div>
            </section>
            <section>
                <h2>Projects</h2>
                <div id="projects">
                    {% for project in posts.projects %}
                        <a href="/blog/{{ project.post_id | slugify }}.html">
                            <div>
                                <div class="project-image" style="background-image: url('{{ project.feature_image }}');"></div>
                                <span>{{ project.name }}</span>
                            </div>
                        </a>
                    {% endfor %}
                </div>
            </section>
            <section>
                <a href="/gallery">
                    <div class="content-bubble gallery">
                        <h2 class="content">View Gallery</h2>
                        <div class="feature-image" style="background-image: url('/assets/gallery/house-of-loud-min.gif');"></div>
                    </div>
                </a>
            </section>
            <section id="tags">
                <h2>Tags</h2>
                <ul class="commas">
                    {% for tag in posts.all_tags %}
                        <li>
                            <a href="/tags/{{ tag | slugify }}.html">{{ tag }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </section>
        </main>
        <script src="/src/scripts.js"></script>
    </body>
</html>